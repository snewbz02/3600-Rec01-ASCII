/*
Created by Sienna Newby-Sanchez

Goal: Create a program that will prompt the user to enter
an ASCII printable character and then print out its decimal
(base 10), hexadecimal (base 16) and all 8 binary digits of
its binary (base 2) representation.

This will include characters such as:
A, B, C, #, $, &, +, 0, 1, 2, <, a, b, c, and so on. 

Class: 3600.004-208
*/

#include <stdio.h>

int main() {
  unsigned char input_char;

  //Prompt the user to enter an ASCII character
  printf ("Enter an ASCII character: ");
  scanf ("%c", &input_char);
  
}
